import { PageIntro } from "@/components/page-intro";

export default function CookiesPage() {
  return (
    <PageIntro eyebrow="Legal" title="Cookie policy">
      <p>
        Describe essential cookies, analytics (if any), marketing pixels, and how users can manage preferences. Keep in
        line with your cookie banner implementation.
      </p>
    </PageIntro>
  );
}
